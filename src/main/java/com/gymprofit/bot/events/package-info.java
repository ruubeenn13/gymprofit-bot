/**
 * Listeners de eventos de JDA: bienvenida y auto-roles, XP por mensaje (cooldown 60 s),
 * interacciones de botones y select menus, auto-mod.
 *
 * <p>Los listeners delegan la lógica de negocio en {@code services} (no la implementan
 * en línea) para poder testearla con JUnit + Mockito.</p>
 */
package com.gymprofit.bot.events;
